[ -z "$1" ] && exit 0

if [ "$1" = "-i" ] && [ -n "$2" ]
then
	wpctl set-volume @DEFAULT_SINK@ "$2"
elif [ "$1" = "-t" ]
then
	wpctl set-mute @DEFAULT_SINK@ toggle
elif [ "$1" = "-m" ]
then
	wpctl set-mute @DEFAULT_SINK@ 1
fi
